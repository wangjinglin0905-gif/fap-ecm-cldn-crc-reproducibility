# Raw-data directory

This directory is intentionally empty in the distributed archive. Follow `../docs/DATA_DOWNLOAD.md` and `../DATA_MANIFEST.csv` to acquire public datasets from their authoritative repositories.

Do not commit raw public datasets by default. Commit only small, licence-compatible derived source tables, acquisition metadata, inclusion maps and checksums.
